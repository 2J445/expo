<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
<% else %>
  <div class="bg-secondary p-3 p-sm-5 my-4 rounded">
    <div class="text-center text-white">
      <h1>アプリ概要</h1>
      <p>サインアップは<%= link_to 'こちら', signup_path, class: 'btn btn-lg btn-primary' %></p>
    </div>
  </div>
<% end %>