<div class="favorite-list">
<h3>お気に入り一覧</h3>
<% @likes.each do |post| %>

<div class="post-box">
<p><%= link_to image_tag(post.photo.url, :alt => 'お気に入り登録している写真'), post %></p>

<div class="like-post-information">
<h5>作品名</h5>
<p><%= post.title %></p>

<h5>作者</h5>
<p><%= link_to post.user.name %></p>

<h5>詳細</h5>
<p><%= post.explanation %></p>
</div>
</div>
<% end %>

<% if @likes.count == 0 %>
<div class="nil-like-post">
    <p>お気に入りにしている現在、作品はありません。</p>
</div>
<% end %>
<div class="pagy">
<%== pagy_bootstrap_nav(@pagy) %>
</div>
</div>