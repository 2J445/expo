<h3>作品一覧</h3>
<div class="row">
      <% @posts.each do |post| %>
      <p><%= link_to image_tag(post.photo.url), post, size: '300x600' %></p>
      <% end %>
      <%== pagy_bootstrap_nav(@pagy) %>
  </div>
